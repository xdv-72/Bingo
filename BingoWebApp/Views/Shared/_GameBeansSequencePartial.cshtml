@model List<int>

<div class="xTable" style="width:400px;">
    <div class="xRow" style="height: 140px;">
        <div id="beans-cell-owner" class="xCell light-border" style="padding-top: 10px;">
            @for (int i = 0; i < Model.Count; i++)
            {
                <span class="beans-cell">
                    @Model[i]
                </span>
            }
        </div>
    </div>
</div>